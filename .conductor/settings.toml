"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
setup = "npm ci"
run_mode = "concurrent"

[scripts.run.test]
command = "npm run test:watch"
default = true
icon = "test-tube"

[scripts.run.types]
command = "npm run dev"
icon = "braces"
